@model IEnumerable<WebInfos.Model.TbEvento>

@{
    ViewData["Title"] = "Evento";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                        <th>
                @Html.DisplayNameFor(model => model.IdEve)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Evento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
                        <td>
                @Html.DisplayFor(modelItem => item.IdEve)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Evento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdEve">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdEve">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdEve">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
